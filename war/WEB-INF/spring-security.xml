<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:security="http://www.springframework.org/schema/security"    
    xsi:schemaLocation="
    	http://www.springframework.org/schema/beans 
    	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd                
        http://www.springframework.org/schema/security 
        http://www.springframework.org/schema/security/spring-security-3.2.xsd">
	
	<!-- Habilitamos las anotaciones de spring security -->
	<global-method-security secured-annotations="enabled" pre-post-annotations="enabled" />
	
	<!-- Stateless RESTful services use BASIC authentication -->
    <http auto-config="false" create-session="stateless"
    	  entry-point-ref="restEntryPoint" disable-url-rewriting="true" 
    	  security-context-repository-ref="restSecurityContextRepository" use-expressions="true" >
		<intercept-url pattern="/login" access="permitAll" requires-channel="https"/>
		<intercept-url pattern="/info/*" access="permitAll" requires-channel="https"/>
		<!-- Particularizamos la direccion de login y los nombres de los parametros de usuario y contraseña 
						"	
		-->
		<form-login
        	login-processing-url="/login"
			username-parameter="user"
			password-parameter="password"
        	authentication-success-handler-ref="restAuthenticationSuccessHandler"
         	authentication-failure-handler-ref="restAuthenticationFailureHandler"
      	/>       
    </http>
 
	<beans:bean id="restSecurityContextRepository" class="com.vfg.appengineblank.commons.security.RestcacheSecurityContextRepository"  /> 
 	<beans:bean id="restEntryPoint" class="com.vfg.appengineblank.commons.security.RestEntryPoint" />
 	
 	<beans:bean id="restAuthenticationSuccessHandler" class="com.vfg.appengineblank.commons.security.RestAuthenticationSuccessHandler" />
 	<beans:bean id="restAuthenticationFailureHandler" class="org.springframework.security.web.authentication.SimpleUrlAuthenticationFailureHandler" />
 	
    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="vfg" password="123abc" authorities="ROLE_REST"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>
 
</beans:beans>